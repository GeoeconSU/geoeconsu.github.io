"I now have a complete, verified picture. Here is the ground-truth inventory.\n\n## 1. Pages that exist on disk\n\n**Top-level:** index.html (90KB, homepage), index3.html (93KB, alt homepage), dashboard.html (937KB, the RAO analytics app — by far the largest file), admin.html (113KB, internal ops/leads/user-plan console), tracker.html (95KB, internal task tracker), map.html (17KB), rao.html (36KB), barometer.html (25KB, GRB report gate landing), leads.html (48KB), contact.html, privacy.html, terms.html, fixed.html, podcast.html/-bg/-cover, email_grb_q2_2026.html (cold-email template). No console.html exists.\n\n**dev/**: chat_dev.html, console_dev.html, dashboard_dev.html, dashboard_old.html, dashboard_v2.html, test.html — clearly working/legacy copies, not production pages.\n\n**pitches/**: angelone.html, igpl.html, igplscroll.html, plus **pitches/igpl/** — 44 near-identical ~91KB company pitch pages (paytm.html, razorpay-adjacent mentions, billdesk.html, juspay.html, pine-labs.html, instamojo.html, etc.) generated from a shared template.html. These reference \"payments\"/\"Razorpay\" only as sponsorship-prospect industry descriptors inside sales-deck copy — not integrations.\n\n**Payment-keyword grep results:** No file anywhere contains stripe, checkout, or an actual payment SDK/integration. Hits found are all cosmetic/copy: admin.html says \"active subscriptions\" (a label for approved plans, no billing), dashboard.html explicitly states \"No payment now · our team will be in touch to finalise\" in its upgrade modal, index3.html has a `mailto:` \"Request Subscription\" link, tracker.html has a code comment \"Subscriptions\" section (unrelated internal feature), and privacy.html states \"We do not collect payment card details through the platform.\"\n\n## 2. Access control (firebase-config.js)\n\nTwo independent axes: `level` (1=director/leadership … 5=free registered user, 6+ reserved) drives internal staff tooling (admin.html, tracker.html), via `levelAtMost(n)`. `plan` ('free'|'personal'|'enterprise') drives dashboard content, via `effectivePlan()`/`planAtLeast()`. Level ≤4 (staff + legacy \"client\") is auto-treated as enterprise regardless of `plan`. New signups get `level:5, plan:'free'` by default.\n\nIn dashboard.html, `TAB_ACCESS` gates tabs: overview=free, analysis/compare/rankings/insights=personal, analytics=enterprise. `refreshTabLocks()` blurs locked tabs and shows an \"Unlock with Personal/Enterprise\" button that opens `openUpgradeModal()` → `requestPlan()`, which just writes `requestedPlan/planStatus:'pending'` to the user's Firestore doc — no charge occurs. An admin then manually `approvePlan()`/`declinePlan()`s it in admin.html (lines ~1548-1576), flipping `planStatus`/`plan` fields directly. **This confirms: any signed-in user sees the full dashboard shell; content is gated purely by Firestore `plan`/`level` flags, and upgrades today are a manual sales-approval workflow, not a checkout.**\n\n## 3. gsu-gate/src/index.js (Cloudflare Worker) — confirmed\n\nSingle Worker handling multiple unrelated endpoints, none of which are payment: `/send-report` (lead-capture gate: validates name+email, whitelists exactly 3 PDFs — `ALLOWED_PDFS = {'GRB Q2 2026.pdf','GRB Q1 2026.pdf','GRB Q4 2025.pdf'}` under docs/, emails the download link via Resend, and writes the lead to Firebase RTDB at `gate_leads/`), `/track` + `/track-email` + `/track-open` (RTDB event/click/open logging for the barometer email campaign), `/search` (Tavily), `/gemini-chat` + `/gemini-stream` (chatbot proxy, unrelated to gating), `/get-leads` (auth'd read of leads for leads.html/admin), `/send-cold-email`, `/contact` (contact form → Resend). Called via `GATE_WORKER = 'https://gsu-gate.guduruadip.workers.dev'` from index.html (line 2035) and barometer.html (line 582), both POSTing to `/send-report`.\n\n## 4. briefs.json / insights/\n\nFully public, no gating at all. `load-briefs.js` fetches briefs.json and renders links (`target=\"_blank\"`) pointing directly at `insights/*.pdf`. Anyone can hit those PDF URLs directly with no lead form, no auth, no gsu-gate call — distinct from the GRB reports under docs/, which are gated.\n\n## 5. Business-model summary\n\nNo pricing/plans page exists on the public site. The only monetization-adjacent surfaces are: (a) the GRB report lead-capture gate (name/email harvest, not payment), and (b) dashboard.html's plan tiers (free/personal/enterprise) with a \"Request Access\" → manual admin approval flow, explicitly marketed as \"no payment now.\" There is currently zero payment-gateway code, no Stripe/Razorpay/PayPal keys, and no checkout UI anywhere in the repo.